# Agent instructions

The Next.js app lives in `Mundoko-Maps/`. Project intent is in `README.md`; app setup is in `Mundoko-Maps/README.md`.

## Agent skills

### Issue tracker

GitHub Issues on `LL782/mundoko-maps`, via `gh`. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical roles with matching label strings (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` plus `docs/adr/` at the repo root. See `docs/agents/domain.md`.
