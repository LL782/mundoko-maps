import type { NextPage } from "next";
import { MapSlice } from "../MapSlice";

const MapSlicePage: NextPage = () => {
  return (
    <div>
      <MapSlice />
    </div>
  );
};

export default MapSlicePage;
